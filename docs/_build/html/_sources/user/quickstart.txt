Quickstart
==========

When every :ref:`requirements <requirements>` are met, you'll need first to launch the X server::

    $ startx
    $ export DISPLAY=:0.0

Then, you can launch a game using the script main.py::

    $ python main.py

